apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Values.redis.name}}
  labels:
    app: {{.Values.redis.name}}
spec:
  replicas: {{.Values.redis.replicas}}
  selector:
    matchLabels:
      app: {{.Values.redis.name}}
  template:
    metadata:
      labels:
        app: {{.Values.redis.name}}
    spec:
      containers:
      - name: app
        image: "{{ .Values.redis.image }}"
        ports:
        - containerPort: {{.Values.redis.port}}
        env:
        - name: PORT
          value: {{ .Values.redis.port | quote }}
        {{- range $key, $value := .Values.redis.envVar }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{.Values.redis.name}}
spec:
  type: ClusterIP           
  selector:
    app: {{.Values.redis.name}}
  ports:
    - protocol: TCP
      port: {{ .Values.redis.port }}
      targetPort: {{ .Values.redis.port }}
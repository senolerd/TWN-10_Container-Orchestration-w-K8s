---
apiVersion: v1
kind: Service
metadata:
  name: mongo
spec:
  type: NodePort
  selector:
    db: mongo
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
# ---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: mymongo-com-httproute
#   labels:
#     gateway: gateway-ext
# spec:
#   parentRefs:
#   - name: gateway-ext # <--- needs to be changed based on Gateway 
#   hostnames:
#     - mymongo.com
#   rules:
#   - matches:
#     - path:
#         type: PathPrefix
#         value: /
#     backendRefs:
#       - name: mongo
#         port: 8081

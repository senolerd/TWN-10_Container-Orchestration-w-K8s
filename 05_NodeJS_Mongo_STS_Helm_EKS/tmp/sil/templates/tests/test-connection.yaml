apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sil.fullname" . }}-test-connection"
  labels:
    {{- include "sil.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sil.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
